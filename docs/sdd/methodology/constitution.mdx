---
title: "Constitution"
description: "The five principles that bound everything in this repo."
icon: "scroll-text"
---

<p className="cf-kicker">SOURCE OF TRUTH</p>

The canonical document lives at
[`.specify/memory/constitution.md`](https://github.com/ThiagoPanini/campfire/blob/main/.specify/memory/constitution.md).
This page summarizes it. When the two disagree, the canonical file wins.

<Info>
  Current version: <span className="cf-label">VERSION</span> 1.1.0 &nbsp;
  <span className="cf-label">RATIFIED</span> 2026-04-24 &nbsp;
  <span className="cf-label">LAST AMENDED</span> 2026-04-26
</Info>

## Principles

<AccordionGroup>
  <Accordion title="I. Narrow MVP scope" icon="target">
    The MVP serves three jobs only: record songs the user knows, capture songs
    the user is learning, and share that repertoire with a small circle.
    Everything else is out of scope until an amendment moves it in. **YAGNI is
    the default answer.**
  </Accordion>
  <Accordion title="II. Incremental delivery" icon="layers">
    Work ships in thin, demonstrable slices in this order: frontend → backend
    → LocalStack → AWS infrastructure (Terraform) → GitHub Actions
    deployment. Earlier stages must be usable, even with mocked data, before
    later ones begin.
  </Accordion>
  <Accordion title="III. Boring, proven stack" icon="anchor">
    React + TypeScript + Vite on the frontend. Python + `uv` + FastAPI +
    relational DB on the backend, with Clean / Hex / DDD applied
    pragmatically. AWS-native infra in Terraform. Docker + LocalStack for
    local validation. GitHub Actions for CI/CD. Mintlify for docs. New
    languages, frameworks, or providers require an amendment.
  </Accordion>
  <Accordion title="IV. Proportional rigor" icon="scale">
    Tests, accessibility, observability, and process show up when a real
    trigger justifies them: a recurring bug class, a regression, an incident,
    a compliance requirement, or logic complex enough that a test is the
    cheapest way to reason about it. "We might need it later" is not a
    trigger.
  </Accordion>
  <Accordion title="V. Docs-as-code, continuously" icon="file-text">
    Every user-facing or architectural change ships its Mintlify update in the
    same change set. If a change needs no doc update, that must be obvious
    from the change itself.
  </Accordion>
</AccordionGroup>

## Hard constraints

- Domain layer must not import frameworks, ORMs, or AWS SDKs.
- Secrets never committed. Local via `.env`; cloud via AWS-native stores.
- Relational DB by default. No NoSQL, queues, or caches without a feature
  that demonstrably requires them.
- Solo + AI default. The maintainer reviews every change.

## Backend architecture invariants

Added in **v1.1.0** after slice `002-backend-auth-slice`. Mechanism is
documented in [ADR-0006](https://github.com/ThiagoPanini/campfire/blob/main/specs/002-backend-auth-slice/adr/0006-backend-hexagonal-ddd-pattern.md)
and the [backend architecture playbook](/backend/architecture).

<Steps>
  <Step title="Slice by bounded context">
    Backend code lives under `apps/api/src/campfire_api/contexts/<context>/`
    with its own `domain/`, `application/`, and `adapters/`.
  </Step>
  <Step title="Layer purity is a test, not a review note">
    An automated test fails the build if `domain/` or `application/` imports
    a framework, ORM, password library, JWT library, or cloud SDK.
  </Step>
  <Step title="Cross-context references go by identifier">
    A context never imports another context's entities, rows, or
    repositories. Identifier value objects (e.g. `UserId`) cross the line.
  </Step>
  <Step title="Errors become HTTP only at the edge">
    Use cases raise domain exceptions; a single registered FastAPI handler
    maps them to status codes. No `HTTPException` inside use cases.
  </Step>
  <Step title="Transactions have an explicit owner">
    The session / Unit of Work is opened and committed by the adapter that
    invokes the use case, never by the use case itself.
  </Step>
  <Step title="Validation lives where it protects">
    Pydantic guards the transport contract; value objects guard the domain.
    No rule is enforced in more than two places.
  </Step>
  <Step title="Settings and time are ports">
    `SettingsProvider` and `Clock` Protocols replace `os.getenv` and
    `datetime.utcnow()` in `domain/` and `application/`.
  </Step>
</Steps>

## Amending

The maintainer may amend at any time. Amendment commits must:

1. Update the version line per the rules — MAJOR / MINOR / PATCH.
2. Update `Last Amended`.
3. Include a one-paragraph rationale in the commit message.

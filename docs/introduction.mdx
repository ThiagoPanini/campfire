---
title: "Introduction"
description: "Campfire is a music hub for groups of friends who meet for amateur jam sessions."
---

Campfire is a **private alpha** music hub for small groups of friends who organize amateur jam sessions. Members track their personal repertoire by instrument, capture what they want to practice, and share their set with the group before sessions start.

The codebase is a monorepo with a React/Vite frontend, a Python Lambda backend, and Terraform modules for AWS-native infrastructure.

**MVP scope (active):** The `003-refactor-auth-preferences` slice delivers the full self-service auth and onboarding path — Cognito email/password sign-up and sign-in, Google OAuth via redirect, email verification, single Campfire account per verified email, preferences onboarding, and the returning-user home flow. There is no mock-session acceptance path in the MVP; all auth flows go through Cognito.

## Where to start

<Columns cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Clone the repository, start LocalStack, and run the backend and frontend locally.
  </Card>
  <Card title="Local environment" icon="terminal" href="/guides/platform/local-environment">
    Use the Makefile workflow for local services, tests, debugger attach, and cleanup.
  </Card>
  <Card title="Auth bootstrap" icon="shield-check" href="/concepts/auth-bootstrap">
    Read how identity, sessions, and the `/me` bootstrap work.
  </Card>
  <Card title="API reference" icon="code" href="/api-reference/introduction">
    Browse the `/health` and `/me` endpoints generated from the OpenAPI contract.
  </Card>
</Columns>

## Repository layout

| Path | Purpose |
|------|---------|
| `apps/web` | React + Vite frontend with public landing, auth callback, protected shell, and `/me` screen |
| `apps/api` | Python Lambda-style backend with `/health`, `/me`, claims mapping, and local-user bootstrap |
| `apps/shared/contracts` | Implementation-facing API contracts |
| `infra/terraform` | AWS modules for DNS, hosting, Cognito, API Gateway, Lambda, DynamoDB, and observability |
| `specs/001-auth-bootstrap` | Delivered auth-bootstrap feature artifacts |
| `specs/002-cicd-local-env` | Active local environment and CI/CD platform artifacts |
| `scripts/local` | Canonical local workflow scripts used by `make` |
| `docs/guides/platform` | Local environment, CI/CD, and operations documentation |

## Project status

Campfire is under active development. The implemented surface is still foundation-level: authentication, protected entry, local user bootstrap, and local platform workflow. Music-domain features like jam sessions, songs, instruments, and group membership are intentionally future work.

# Campfire API local environment
#
# Copy this file to apps/api/.env, then fill the blank secret values. Keep
# apps/api/.env out of git.

# Core runtime
ENV=dev
LOG_LEVEL=INFO
DATABASE_URL=postgresql+asyncpg://campfire:campfire@localhost:5432/campfire
CORS_ORIGINS=http://localhost:5173
TRUSTED_PROXIES=
WEB_BASE_URL=http://localhost:5173

# Session and cookies
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=1209600
REFRESH_COOKIE_NAME=campfire_refresh
REFRESH_COOKIE_DOMAIN=
REFRESH_COOKIE_SECURE=false
REFRESH_COOKIE_SAMESITE=lax

# Google OAuth
#
# Create a Google OAuth web client with this authorized redirect URI:
# http://localhost:8000/auth/google/callback
GOOGLE_STUB_ENABLED=false
GOOGLE_OAUTH_ENABLED=true
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/auth/google/callback
# Generate with: openssl rand -hex 32
OAUTH_FLOW_HMAC_KEY=
OAUTH_FLOW_TTL_SECONDS=600

# Email confirmation
EMAIL_CONFIRMATION_REQUIRED=true
# Generate with: openssl rand -hex 32
EMAIL_CONFIRMATION_HMAC_KEY=
EMAIL_CONFIRMATION_TTL_SECONDS=900
EMAIL_CONFIRMATION_MAX_ATTEMPTS=5
EMAIL_CONFIRMATION_RESEND_COOLDOWN_SECONDS=60
EMAIL_CONFIRMATION_RESEND_HOURLY_CAP=3

# Mailer
#
# console writes confirmation codes to MAIL_OUTBOX_DIR instead of sending real
# email. For local testing, inspect the newest *-confirmation.txt file there.
# Use MAIL_BACKEND=http with MAIL_HTTP_URL and MAIL_HTTP_API_KEY to test real
# transactional delivery.
MAIL_BACKEND=console
MAIL_FROM=noreply@campfire.local
MAIL_OUTBOX_DIR=tmp/mail
MAIL_HTTP_URL=
MAIL_HTTP_API_KEY=

# Auth rate limiting
RATE_LIMIT_PER_WINDOW=10
RATE_LIMIT_WINDOW_SECONDS=300

# Repertoire search
DEEZER_BASE_URL=https://api.deezer.com
SEARCH_CACHE_TTL_SECONDS=60
SEARCH_CACHE_MAX_ENTRIES=1024
SEARCH_RATE_LIMIT_PER_WINDOW=30
SEARCH_RATE_LIMIT_WINDOW_SECONDS=60

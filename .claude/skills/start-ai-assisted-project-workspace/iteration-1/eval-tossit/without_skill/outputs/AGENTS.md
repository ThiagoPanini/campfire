# AGENTS.md

Pointer file for non-Claude coding agents (Codex, Cursor, Aider, etc.).

All conventions for AI sessions in this repository live in [CLAUDE.md](./CLAUDE.md). Read that file first; everything in it applies regardless of which agent or IDE is driving the session.

Workflow-level guidance — when to add tooling, how MCPs and sub-agents are introduced — is in [AI_WORKFLOW.md](./AI_WORKFLOW.md).

If something in CLAUDE.md is Claude-specific and does not apply to your agent, treat it as a no-op rather than translating it.
